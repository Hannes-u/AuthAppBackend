version: "3.7"
services:
    mysql-db:
      image: mysql:latest
      restart: always
      environment:
        MYSQL_ROOT_PASSWORD: password
        MYSQL_DATABASE: userService
      ports:
        - "3306:3306"
      volumes:
        - my-db:/var/lib/mysql